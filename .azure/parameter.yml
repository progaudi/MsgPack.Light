parameters:
  name: ''
  value: ''

steps:
- bash: |
    if [ -z "$VALUE" ]; then
      echo ##vso[task.complete result=Failed;]Missing template parameter \"name\"
    fi
  env:
    VALUE: ${{ parameters.value }}